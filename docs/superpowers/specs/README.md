# Historical Specs

This folder stores dated design and specification artifacts from earlier planning stages.

These documents remain useful as historical context, but they are not authoritative for the current repository state.

For current implementation truth, use the active docs at the repository root and the codebase itself.
