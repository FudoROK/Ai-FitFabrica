"""Identity core contracts package.

Intentionally does not re-export contract symbols to avoid widening import entry points.
Consumers must use explicit module-level imports, e.g.:
`from src.identity_core.contracts.lead_repository import LeadRepository`.
"""

__all__: list[str] = []
